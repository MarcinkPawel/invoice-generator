import { CustomerFrom } from "../CustomerForm";
import { CustomerList } from "../CustomerList";

export const Main = () => {
  return (
    <div>
      <CustomerFrom />
      <CustomerList />
    </div>
  );
};
